/**
 * Workspace page — the main entry point.
 *
 * The AI chat interface is rendered by WorkspaceLayout in layout.tsx
 * when the user is on /workspace. This page component exists to satisfy
 * Next.js App Router requirements.
 */
export default function WorkspacePage() {
  return null;
}
